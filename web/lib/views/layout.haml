!!!*
%html
  %head
    %meta(charset='utf-8')
    %title Todo Application
    %link(href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css' rel='stylesheet')
    %link(href='/css/main.css' rel='stylesheet')
  %body
    %nav.navbar.navbar-default
      .container
        .navbar-header
          %button.navbar-toggle.collapsed(data-target='#bs-example-navbar-collapse-1' aria-expanded='false')
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand(href='/tasks')
            Todo Application

        .collapse.navbar-collapse#bs-example-navbar-collapse-1
          %ul.nav.navbar-nav
            %li{class: ('active' unless @status)}
              %a(href='/tasks') ALL TASKS
            %li{class: ('active' if @status == 'not_yet')}
              %a(href='/tasks?status=not_yet') NOT YET
            %li{class: ('active' if @status == 'pending')}
              %a(href='/tasks?status=pending') PENDING
            %li{class: ('active' if @status == 'done')}
              %a(href='/tasks?status=done') DONE

    .container
      != yield